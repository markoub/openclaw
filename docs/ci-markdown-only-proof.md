# Markdown-only CI proof

This temporary document exercises the Markdown-only pull request routing added
by `markoub/markdown-only-ci-scope`.

The branch intentionally changes only Markdown under `docs/` so the CI scope
gate can classify the pull request as docs-only.
